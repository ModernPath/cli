# Local build outputs.
#
# Untracking them is necessary but not sufficient: the release rsync copies
# the filesystem rather than git's index, so the release procedure in
# ../../AGENTS.md excludes these same names explicitly. Keep the two lists in
# step.
modernpath
modernpath-new
cli
